[build]
builder = "RAILPACK"
buildCommand = "pnpm versions && pnpm preflight && pnpm build:worker"

[deploy]
startCommand = "node .runtime/worker/dist/main.js"
healthcheckPath = "/health/ready"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
