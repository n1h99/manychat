[build]
builder = "RAILPACK"
buildCommand = "corepack enable && corepack prepare pnpm@10.5.0 --activate && pnpm install --frozen-lockfile && pnpm build:worker"

[deploy]
startCommand = "pnpm start:worker"
healthcheckPath = "/health/ready"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
