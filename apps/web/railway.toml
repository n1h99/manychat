[build]
builder = "RAILPACK"
buildCommand = "pnpm versions && pnpm preflight && pnpm build:web"

[deploy]
startCommand = "node .runtime/web/server.mjs"
healthcheckPath = "/health/ready"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
